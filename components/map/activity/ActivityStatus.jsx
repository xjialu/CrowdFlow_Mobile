import React, { useState } from 'react';
import {
  Alert,
  Modal,
  StyleSheet,
  Text,
  Pressable,
  View
} from 'react-native';
import styles from './activitystatus.style';
import { useRouter } from 'expo-router';

const ActivityStatus = () => {
  return (
    <View
      style={styles.background}
    />
  )
};

export default ActivityStatus;